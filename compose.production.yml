name: zemazap

services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-zemazap}
      POSTGRES_USER: ${POSTGRES_USER:-zemazap}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-zemazap} -d ${POSTGRES_DB:-zemazap}"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks: [backend]

  django:
    build:
      context: .
      dockerfile: backend/Dockerfile
    restart: unless-stopped
    env_file: [backend/.env]
    environment:
      DJANGO_DEBUG: "false"
      DATABASE_URL: postgresql://${POSTGRES_USER:-zemazap}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-zemazap}
      DATABASE_SSL_REQUIRE: "false"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/health/', timeout=5)"]
      interval: 15s
      timeout: 5s
      retries: 10
    networks: [backend, edge]

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    restart: unless-stopped
    env_file: [.env]
    environment:
      ZEMAZAP_DJANGO_API_URL: http://django:8000
    depends_on:
      django:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:3000/api/health"]
      interval: 15s
      timeout: 5s
      retries: 10
    ports:
      - "127.0.0.1:3000:3000"
    networks: [edge]

volumes:
  postgres-data:

networks:
  edge:
  backend:
    internal: true
